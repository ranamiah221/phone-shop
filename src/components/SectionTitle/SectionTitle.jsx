

const SectionTitle = ({titleText}) => {
    return (
        <div className="text-center text-2xl font-semibold mt-5 mb-5">
            <h2>{titleText}</h2>
        </div>
    );
};

export default SectionTitle;